<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 应用信息
    |--------------------------------------------------------------------------
    */
    'app' => [
        'name' => env('APP_NAME', 'NativePHP ThinkPHP'),
        'version' => env('APP_VERSION', '1.0.0'),
        'icon' => null,
    ],

    /*
    |--------------------------------------------------------------------------
    | 窗口设置
    |--------------------------------------------------------------------------
    */
    'window' => [
        'width' => 800,
        'height' => 600,
        'center' => true,
        'title' => env('APP_NAME', 'NativePHP ThinkPHP'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 菜单设置
    |--------------------------------------------------------------------------
    */
    'menu' => [
        'enabled' => true,
    ],

    /*
    |--------------------------------------------------------------------------
    | 系统托盘设置
    |--------------------------------------------------------------------------
    */
    'tray' => [
        'enabled' => false,
        'icon' => null,
        'tooltip' => env('APP_NAME', 'NativePHP ThinkPHP'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 快捷键设置
    |--------------------------------------------------------------------------
    */
    'hotkeys' => [
        // 全局快捷键配置
        'global' => [
            'toggle-window' => 'CommandOrControl+Shift+T',
            'show-settings' => 'CommandOrControl+,',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 开发者工具
    |--------------------------------------------------------------------------
    */
    'dev' => [
        'enabled' => env('APP_DEBUG', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | 安全设置
    |--------------------------------------------------------------------------
    */
    'security' => [
        'csp' => [
            'default-src' => ["'self'"],
            'script-src' => ["'self'"],
            'style-src' => ["'self'"],
            'img-src' => ["'self'", 'data:'],
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 更新器设置
    |--------------------------------------------------------------------------
    */
    'updater' => [
        // 是否启用自动更新
        'enabled' => false,
        
        // 更新服务器地址
        'server' => env('NATIVE_UPDATE_SERVER', ''),
        
        // 更新验证公钥
        'pubkey' => env('NATIVE_UPDATE_PUBKEY', ''),
        
        // 更新通道
        'channel' => env('NATIVE_UPDATE_CHANNEL', 'stable'),
        
        // 更新检查间隔（小时）
        'interval' => env('NATIVE_UPDATE_INTERVAL', 24),
        
        // 更新下载目录
        'download_dir' => runtime_path('updates'),
        
        // 更新安装模式：silent（静默）, prompt（提示）, manual（手动）
        'install_mode' => env('NATIVE_UPDATE_INSTALL_MODE', 'prompt'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 窗口动画设置
    |--------------------------------------------------------------------------
    */
    'transitions' => [
        'enabled' => true,
        'duration' => 300,
        'easing' => 'easeInOutCubic',
        'presets' => [
            'fast' => [
                'duration' => 150,
                'easing' => 'easeOutQuint'
            ],
            'slow' => [
                'duration' => 600,
                'easing' => 'easeInOutQuint'
            ],
            'bounce' => [
                'duration' => 500,
                'easing' => 'easeOutBounce'
            ],
            'elastic' => [
                'duration' => 600,
                'easing' => 'easeOutElastic'
            ]
        ]
    ],
];